#include<iostream>
#include <cmath>
#include <cstdio>
#include <vector>
#include <algorithm>
using namespace std;
struct node {
    int data;
    struct node *link;
};
int main()
{
    struct node *temp=NULL,*root=NULL,*p=NULL;
    int i;
    cout<<"enter the length of linked list: ";
}