#include<iostream>
#include <cmath>
#include <cstdio>
#include <vector>
#include <algorithm>
using namespace std;

struct node{
    int data;
    struct node *link;
};

int main()
{
    struct node *root=NULL, *temp=NULL;
    temp=(struct node *)malloc(sizeof(struct node));
    if (root==NULL)
}