#include<iostream>
#include <cmath>
#include <cstdio>
#include <vector>
#include <algorithm>
using namespace std;
int main()
{
    int i,n,a[50];
    cout<<"enter the size of array: ";
    cin>>n;
    for(i =0;i<n;i++)
    {
        cin>>a[i];
    }
    
    for(i =0;i<n;i++)
    {
        cin>>a[i];
    }
    return 0;
}