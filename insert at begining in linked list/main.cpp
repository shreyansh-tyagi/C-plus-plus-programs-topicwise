#include<iostream>
#include <cmath>
#include <cstdio>
#include <vector>
#include <algorithm>
using namespace std;
struct node
{
    int data;
    struct node *link;
};

int main()
{
    int i,n,a;
    struct node *root=NULL,*temp=NULL,*p=NULL;
    cout<<"enter the size of linked list: ";
    cin>>n;
    for(i=0;i<n;i++)
    {

    }

    
    return 0;
}